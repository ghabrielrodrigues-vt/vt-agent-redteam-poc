# Secrets — NEVER commit
.env
.env.local
.env.*.local
*.pem
*.key

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
*.egg
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
venv/
ENV/

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm/

# Promptfoo / PyRIT generated runs (kept under separate dir per tool)
promptfoo/generated/raw-output.yaml
pyrit/generated/last-run.json
pyrit/generated/memory.duckdb

# Cloned upstream agents repo (huge node_modules; clone separately if needed)
livekit-local/livekit-agents/

# Local audio captures + temp WAV files
livekit-local/mock-agent/responses/*.aiff
*.aiff
/tmp/

# Postgres data (Docker volumes mostly handle this)
livekit-local/postgres-local/data/

# macOS noise
.DS_Store
.AppleDouble
.LSOverride

# IDE
.vscode/
.idea/
*.swp
*.swo

# Build artifacts
dist/
build/
*.egg-info/
